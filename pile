/* Biblioteca: java.util.Stack (import.java.util.Stack)
Quando usa a biblioteca, a pilha é um objeto.
StackEmptyException verifica se a pilha está vazia. 
Métodos: 
push(): empilha um objeto
pop(): desempilha
peek(): pega o elemento do topo
Criação de biblioteca de pilha: */

public boolean PilhaVazia()
public boolean PilhaCheia()
public void Empilhar(<tipo> elemento)
public <tipo> Desempilhar()
public <tipo> ElementoTopo()
public void MostrarPilha()

public boolean PilhaVazia()
{
  if (topo == -1)
    {
      return true;
    }
  else
    {
      return false;
    }
}

public boolean PilhaCheia()
{
  if (topo >= -1)
    {
      return true;
    }
  else
    {
      return false;
    }
}

public void Empilhar (int elemento)
  {
    if (! PilhaCheia())
      {
        topo = topo + 1;
        vetor [topo] = elemento;
      }
    else
      {
        System.out.println ("Pilha Cheia");
      }
  }

public int Desempilhar ()
  {
    int desempilhado = 0;
    if (PilhaVazia())
      {
        System.out.println ("Pilha Vazia");
      }
    else
      {
        desempilhado = vetor[topo];
        topo = topo - 1;
      }
    return desempilhado;
  }

public void ElementoTopo()
  {
    if (topo >=0)
      {
        System.out.println("O elemento do topo é" + vetor[topo]);
      }
    else
      {
        System.out.println("Pilha Vazia");
      }
  }

public void MostrarPilha()
  {
    int i;
    for (i = topo; i >= 0; i--)
      {
        System.out.println("Elemento" + vetor[i] + "posição" + i + "da Pilha");
      }
  }







